import {
  getMaxProductionBudgetMovies,
  getMoviesGroupedByGenres,
} from './homework';

/**
 * Import functions you want to be included into the main JS bundle.
 * This code will be executed as soon as the bundle is loaded
 */

console.log(getMaxProductionBudgetMovies(3));
console.log(getMoviesGroupedByGenres());
