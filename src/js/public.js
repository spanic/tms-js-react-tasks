/**
 * Import you functions those you want to make visible in html (for example, event handlers).
 * These functions will be accessible via my.{%FUNCTION_NAME%} and will be taken from /public/public.js
 */

import { onClick } from './lesson_1/basics';

export { onClick };
